import React from 'react';
import PageTitle from '../PageTitle/PageTitle';

function NoMatch(props) {
  return (
    <div className={StyleSheet.noMatch}>
      <PageTitle>404 NOT FOUND</PageTitle>
    </div>
  );
}

export default NoMatch;
